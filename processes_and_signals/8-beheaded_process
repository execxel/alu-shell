#!/usr/bin/env bash
# Sends SIGTERM to the 7-highlander process

pid=$(pgrep -f '7-highlander' | head -n1)
if [ -n "$pid" ]; then
  kill -TERM "$pid" && echo "Sent TERM to $pid"
else
  echo "No 7-highlander process found"
fi
